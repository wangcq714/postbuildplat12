V1.2：默认诊断路由放在最后若干行，且前面没有诊断CAN节点（如TBOX）发出的报文；诊断CAN名必须位DCAN

V1.3：通过给程序传参转换指定行行,且要将所有诊断路由放在最后；

V1.4 通过pandas模块打开Excel文件，提升程序运行速度；

V1.5 增加一列DiagMsg，用于判断该行是否转换，Y不转  N转；

V1.6 将诊断请求报文转换生成另外一张表，适配GAW1.2新平台；